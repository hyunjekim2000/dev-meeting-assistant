# ETT Issue Tracker Configuration
# Copy this to .env and fill in your values before building

# API URL for your ETT issue tracker instance
# Users will log in at runtime when enabling ticket generation
NEXT_PUBLIC_ETT_API_URL=https://your-ett-api.example.com
